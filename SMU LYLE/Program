CASE

WHEN (REGEXP_MATCH(Landing Page,  ".*(computer-science-ai).*"))
    THEN "MS in Computer Science - AI"
    
WHEN (REGEXP_MATCH(Landing Page,  ".*(network-engineering).*"))
    THEN "MS in Network Engineering"

ELSE "Other"

END
